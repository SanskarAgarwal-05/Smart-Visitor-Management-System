# Backend API Base URL (e.g., deployed Render API url or local fallback)
# Note: For Vite, variables must be prefixed with VITE_ to be exposed to client-side code
VITE_API_URL=https://smart-visitor-backend.onrender.com/api
